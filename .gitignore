
# CyberOS vendored machine + local BRAIN at .cyberos/memory/store (regenerable via init; tenant data). Do not commit.
.cyberos/

# --- QuoteMind build artifacts (do not commit) ---
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
build/
dist/
src_build/
*.egg-info/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage*
htmlcov/
eval/reports/*.json
frontend/node_modules/
frontend/dist/
docs/openapi.json
.DS_Store

# Function Compute build output (`s build`): the vendored runtime dependencies and the s cache.
python/
.s/

# The demo clip and its frames: real screenshots of the deployed UI, rebuilt by
# `python deploy/demo_shots.py && python deploy/demo_clip.py`. Binary, and reproducible.
.demo/
