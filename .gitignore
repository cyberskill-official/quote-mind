

# --- QuoteMind build artifacts (do not commit) ---
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example
build/
dist/
src_build/
*.egg-info/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage*
htmlcov/
eval/reports/*.json
frontend/node_modules/
frontend/dist/
docs/openapi.json
.DS_Store

# Function Compute build output (`s build`): the vendored runtime dependencies and the s cache.
python/
.s/

# The demo clip and its frames: real screenshots of the deployed UI, rebuilt by
# `python deploy/demo_shots.py && python deploy/demo_clip.py`. Binary, and reproducible.
.demo/

# TLS material for the custom domain. The certificate is public, the key is not, and a repo is
# not a key store - re-issue with `python deploy/issue_cert.py` instead of committing either.
deploy/certs/

# >>> cyberos (managed block - regenerated by cyberos install; edit outside the markers) >>>
# vendored machine + local BRAIN store + render intermediates (regenerable via install; tenant data)
.cyberos/
# skill symlinks -> .cyberos/plugin/skills (regenerable via install)
.claude/skills/ship-tasks
.grok/skills/ship-tasks
.commandcode/skills/ship-tasks
.codex/skills/ship-tasks
.opencode/skill/ship-tasks
# <<< cyberos <<<
